<div id="wrapper">
  <div class="inner">
    <div class="head_link">
      <h2>メッセージ</h2>
    </div>
    <div class="message_room_list">
      <% if @user_rooms.present? %>
        <ul class="list">
          <% @user_rooms.each do |user_room| %>
            <% user = user_room.user %>
            <li class="item">
              <%= link_to message_path(user.id) do %>
                <%= attachment_image_tag user, :icon_image, :fill, 40, 40, fallback: "no-icon.png", size:"40x40", class: "face_icon"  %>
                <%= "#{user.nickname}さんとのチャット" %>
              <% end %>
            </li>
          <% end %>
        </ul>
        <%= paginate @user_rooms %>
        <%= render 'layouts/loader' %>
      <% else %>
        <p>チャットルームはありません</p>
      <% end %>
    </div>
  </div>
</div>
