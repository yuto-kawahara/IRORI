$('.follow_btn_area_<%= @user.id %>').html("<%= j(render 'user/relationships/follow', user: @user) %>");
$('.following_count').html("<%= @user.following.count %>");
$('.follower_count').html("<%= @user.followed.count %>");
