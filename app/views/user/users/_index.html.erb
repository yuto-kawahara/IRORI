<% unless users.blank? %>
  <div class="follow_list">
    <%= render 'user/users/list', users: users %>
  </div>
  <%= paginate users %>
  <%= render 'layouts/loader' %>
<% else %>
  <%= follow %>
<% end %>